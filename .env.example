# Copiar a .env.local y completar con los datos del proyecto de Supabase
# (Project Settings → API). Usar la publishable key, nunca la secret/service_role.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
